<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WhatsAppClone" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whatsAppPrimary</item>
        <item name="colorPrimaryVariant">@color/whatsAppPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/whatsAppPrimary</item>
        <item name="colorSecondaryVariant">@color/whatsAppPrimaryDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.WhatsAppClone.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whatsAppPrimary</item>
        <item name="colorPrimaryVariant">@color/whatsAppPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/whatsAppPrimary</item>
        <item name="colorSecondaryVariant">@color/whatsAppPrimaryDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <!-- Set textAllCaps to false to disable all caps for tab text -->
        <item name="textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="SelectedTextAppearance" parent="TextAppearance.Design.Tab">
        <!-- Set textAllCaps to false to disable all caps for tab text -->
        <item name="textAllCaps">false</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="SelectedIndicatorAppearance" parent="Widget.Material3.NavigationRailView.ActiveIndicator">
        <item name="android:backgroundTint">@color/white</item>
    </style>
</resources>